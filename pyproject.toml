[project]
name = "pixiechess-client"
version = "0.1.0"
description = "Python client for the PixieChess API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27,<1",
    "pydantic>=2.0,<3",
]
